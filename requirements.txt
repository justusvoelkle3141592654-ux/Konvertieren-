# Universal File Converter – Python-Abhängigkeiten
# Installation: pip install -r requirements.txt

# UI
streamlit>=1.35

# Bilder
Pillow>=10.0

# Daten / Tabellen
pandas>=2.0
openpyxl>=3.1

# Dokumente
pdf2docx>=0.5.8       # PDF -> DOCX
python-docx>=1.1      # DOCX lesen/schreiben
pypdf>=4.0            # PDF-Textextraktion
fpdf2>=2.7            # TXT -> PDF

# DOCX -> PDF über Microsoft Word (nur Windows/macOS; unter Linux wird
# stattdessen LibreOffice verwendet, falls installiert)
docx2pdf>=0.1.8; sys_platform == "win32" or sys_platform == "darwin"

# Hinweis: Audio- und Video-Konvertierungen rufen ffmpeg direkt auf.
# ffmpeg ist KEIN Python-Paket und muss separat installiert werden:
#   Windows: winget install ffmpeg
#   macOS:   brew install ffmpeg
#   Linux:   sudo apt install ffmpeg
